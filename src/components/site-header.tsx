"use client";
import Link from "next/link";
import { usePathname } from "next/navigation";
import { useState } from "react";
import { BrandMark } from "./brand-mark";
const links = [{ href: "/about", label: "About" }, { href: "/mentorship", label: "Mentorship" }, { href: "/programmes", label: "Programmes" }, { href: "/resources", label: "Resources" }, { href: "/for-families", label: "For families" }];
export function SiteHeader() { const [open, setOpen] = useState(false); const path = usePathname(); return <header className="sticky top-0 z-40 border-b border-[var(--line)] bg-[var(--paper)]/95 backdrop-blur"><div className="mx-auto flex max-w-7xl items-center justify-between px-5 py-3 sm:px-8"><Link href="/" aria-label="Nexus Risegen Initiative home" onClick={() => setOpen(false)}><BrandMark /></Link><nav aria-label="Primary" className="hidden items-center gap-6 lg:flex">{links.map((link) => <Link key={link.href} href={link.href} aria-current={path === link.href ? "page" : undefined} className="text-sm font-medium text-[var(--muted)] transition hover:text-[var(--blue)]">{link.label}</Link>)}<Link href="/contact" className="rounded-full bg-[var(--ink)] px-4 py-2.5 text-sm font-semibold text-white transition hover:bg-[var(--blue)]">Get in touch</Link></nav><button type="button" aria-expanded={open} aria-controls="mobile-nav" onClick={() => setOpen(!open)} className="rounded-lg p-2 text-[var(--ink)] lg:hidden"><span className="sr-only">{open ? "Close" : "Open"} navigation</span><span aria-hidden="true" className="text-2xl leading-none">{open ? "×" : "☰"}</span></button></div>{open && <nav id="mobile-nav" aria-label="Mobile primary" className="border-t border-[var(--line)] px-5 pb-5 lg:hidden"><div className="mx-auto flex max-w-7xl flex-col pt-2">{[...links, { href: "/contact", label: "Get in touch" }].map((link) => <Link key={link.href} href={link.href} onClick={() => setOpen(false)} aria-current={path === link.href ? "page" : undefined} className="rounded-lg px-3 py-3 text-base font-medium hover:bg-[var(--cream)]">{link.label}</Link>)}</div></nav>}</header>; }
